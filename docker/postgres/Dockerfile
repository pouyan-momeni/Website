FROM postgres:15-alpine

# The official postgres image automatically runs `.sql` and `.sh` scripts placed in /docker-entrypoint-initdb.d/
# This is where we could place our database initialization scripts. For now, the POSTGRES_DB env var handles basics.
